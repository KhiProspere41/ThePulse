# Phase 2 data pipeline (load_historical_data.py, compute_elo.py).
# Separate from requirements.txt because nfl-data-py's own pin (pandas<2.0)
# has no prebuilt wheel for recent Python versions and fails to build from
# source. Install with:
#
#   pip install -r requirements-data.txt
#   pip install --no-deps nfl-data-py==0.3.3
#
# (nfl-data-py's actual code works fine against modern pandas/numpy — only
# its metadata pin is stale — so --no-deps skips that broken constraint.)

pandas==3.0.5
numpy==2.5.2
appdirs==1.4.4
pyarrow==25.0.1

# College football (load_historical_cfb_data.py) talks to the CFBD REST API
# directly via httpx (already in requirements.txt) rather than the official
# `cfbd` SDK — that package still pins pydantic<2, which conflicts with
# FastAPI/pydantic-settings in this project and can't be worked around with
# --no-deps (its generated models use pydantic v1 syntax, not just a stale
# version pin, so it genuinely won't run under pydantic v2).
