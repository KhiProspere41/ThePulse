services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: thepulse
      POSTGRES_PASSWORD: thepulse
      POSTGRES_DB: thepulse
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
